const cartItem = {
    productId: productId,
    size: size,
    price: price,
    quantity: quantity,

    pickupDate: pickupDate,
    pickupTime: pickupTime,

    message: message,
    candles: candles,
}